# CoinSphere 开发规则

## 规则与事实来源

- 开发前先读取 Basic Memory `ai-rules` 的“规则读取入口”，再按入口读取项目和环境规则。
- 事实优先级为：用户当前要求、ADR/API 契约、自动化测试、现有代码、聊天上下文。
- Basic Memory 保存个人偏好、模型路由、规则入口和项目/环境专项事实；本文件只保存仓库级硬约束。
- 模型路由默认使用满足任务的最低成本模型；`sol` 思考等级不低于 `xhigh`，`terra`/`luna` 不低于 `max`；目标模型或等级不可用时不得静默降级。
- ADR 保存架构决策，契约文档保存接口语义，路线图保存能力依赖与退出条件，Runbook 保存操作步骤；动态进度只保存在 GitHub。

## 交付

- 分支使用 `codex/<phase>-<slug>`；PR 标题使用 `[type] 中文描述`。合并默认不执行；用户在当前任务明确授权后，Codex 可在 CI 全绿和最终只读复审通过后合并。
- 一个 PR 交付一个可直接使用、可独立验收和回滚的纵向能力，不为单个校验或内部步骤拆 PR。
- Ready PR 必须以 `main` 为 base；依赖未合并的 stacked PR 只能保持 Draft 并指向父分支。
- Migration 默认跟随所属纵向能力；共享基线、破坏性或跨领域 schema、凭据、风控和订单状态机变更使用独立 PR，并附回滚方案。
- 公共契约、migration、共享配置和依赖锁文件保持单一维护者；最终只读复审由主 Agent 完成。
- 同一交付避免重复执行同一验证；验证层级以质量门禁和用户要求为准。
- 默认不执行本地测试、构建、运行、数据库迁移演练或浏览器验证；除非用户在当前任务明确要求，本地验证由远端 CI 和既有部署流程替代。

## 开发边界

- 项目按全新系统开发，只支持 PostgreSQL 16；不增加旧数据、旧接口、多数据库或未进入当前里程碑的兼容层。
- 正式 Paper 观察前允许重置未投产数据并整理历史 migration；开始记录 Paper 晋级证据前必须冻结 migration 历史，此后只能追加版本。
- 输入校验只放在 API、外部数据、URL、文件、凭据等信任边界；模块和领域不变量仍由拥有模块或数据库约束保证。
- 金融领域时间、数据库和接口统一使用 UTC；进程展示时区可独立配置，领域逻辑不得依赖本地时区。价格、数量、金额和费率使用 Decimal，账务值禁止使用 `float64`，JSON 使用十进制字符串。
- 保持模块化单体；工作流只承担粗粒度编排，不参与逐 K 线计算、交易所私有协议或下单。
- 新增金融接口最终统一到 `/api/v1`；保持模块化单体，新增基础设施或共享抽象必须有已实现需求和明确归属。
- 非平凡代码只注释设计意图、状态转换、并发/事务边界、失败处理和回滚语义。
- 只有状态拥有层记录对应结构化日志，避免调用链重复记录同一事件；日志不得记录密钥、令牌、原始载荷或个人数据。

## 安全与交易

- 真实交易所密钥不得进入代码、日志、测试、Issue、PR、CI 或 AI 上下文。
- Codex 和 CI 不接触真实交易密钥；生产 Release/Deploy 默认不触发，用户在当前任务明确授权后，Codex 可从最新 `main` 触发既有工作流并监控验证；任何情况下不得自动执行真实交易。
- AI、工作流和通用 HTTP 节点不得调用交易所私有接口或绕过风控。
- 新交易能力默认关闭，必须经过对应观察期、证据门禁和用户手工放行；缺少任一显式风控上限时保持禁用。
