# CoinSphere 开发规则

## 规则入口

- 开发任务开始前，先读取 Basic Memory `ai-rules` 项目的 `规则读取入口`，再按入口读取全局、项目、专项和环境规则。
- 事实来源优先级为：用户当前要求、ADR/API 契约、自动化测试、现有代码、聊天上下文。
- 需求或架构决策必须写入仓库文档，不能只保留在对话中。

## 交付流程

- 使用 `codex/<phase>-<slug>` 分支；一个 PR 交付一个可直接使用、可独立验收和回滚的纵向能力，不为单个校验、门禁或内部步骤拆 PR。
- Codex 可以自动运行测试、构建、Lint 和安全扫描，也可以创建分支、提交、推送并创建草稿 PR。
- 用户审查并手工合并每个 PR，禁止自动合并。
- Migration 默认跟随所属纵向能力；只有共享基线、破坏性变更或跨领域 schema 使用独立 PR，并附回滚方案。凭据、风控和订单状态机变更仍使用独立 PR。
- 同一开发波次先集中完成实现，只在波次或 PR 完成后执行一次适用验证和一次只读复审，不在子步骤之间反复插入门禁或审核。
- 每个波次采用一个根 Agent 和最多三个子 Agent；仅当两个以上子任务的依赖与契约已冻结、文件所有权互斥且并行能缩短关键路径时创建子 Agent，否则由根 Agent 直接完成。
- 根 Agent 独占公共契约、migration、共享配置、依赖锁文件、计划文档、波次集成和最终只读复审；子 Agent 不得修改任务卡授权路径之外的文件。
- 任务卡必须写明 base commit、目标、输入输出契约、允许修改路径、禁止修改路径、验收命令和回滚方式。
- 独立切片从同一 base fan-out；存在依赖或文件重叠时使用 stacked PR。堆叠 PR 只能保持 Draft 并指向父分支，上游进入 `main` 后下游变基到 `main` 才能 Ready；所有 Ready PR 的 base 必须是 `main`。

## 开发取舍

- 项目按全新系统开发，允许重置未投产的开发数据；不增加旧 schema、旧数据、旧接口或多数据库兼容层。
- 数据库运行栈只保留 PostgreSQL/TimescaleDB，不再为 SQLite 或 MySQL 编写运行、迁移和验证逻辑。
- 校验只放在 API、外部数据、URL、文件、凭据、金融状态和数据库约束等信任边界；内部函数依赖已经验证的类型和状态，不重复判空、兜底或断言。
- 测试按风险投入：金融 Decimal/UTC、数据库约束与 migration、并发事务、恢复、契约、SSRF/Auth、采集去重补数、订单状态机、风控、账务和对账必须覆盖；CRUD、DTO 映射、简单配置、日志包装、普通 UI 和工作流/通知胶水不强制新增单元测试；Bug 修复只增加一个覆盖根因的回归检查。
- PR 只运行受影响模块的构建和核心测试；PostgreSQL Compose、关键浏览器和跨模块集成每个开发波次集中运行一次，完整供应链扫描只在 `main`、定时任务或发布时运行。

## 注释与日志

- 新增或修改的非平凡代码使用准确的中文注释或 Docstring，只说明设计意图、关键状态转换、并发与事务边界、失败处理和回滚语义；禁止逐行翻译、重复表面行为或编写语法教学。
- 只有拥有状态转换的层记录对应结构化日志，避免调用链重复记录同一事件。日志覆盖必要的启动、停止、外部调用、重试、取消、异常和终态，包含排障所需的资源 ID、Worker ID、状态、耗时和错误分类，但不得记录密钥、令牌、原始载荷、个人数据或运行时环境值。

## 安全边界

- 禁止在代码、日志、测试夹具、Issue、PR 或 CI 中写入真实交易所密钥。
- Codex 和 CI 不接触真实交易所密钥；允许通过用户手工触发的 `production` Environment 工作流连接生产主机并自动部署，但不得自动执行真实交易。
- AI、工作流和通用 HTTP 节点不得直接调用交易所私有接口或绕过风控。
- 新交易能力默认关闭，必须经过模拟盘、观察期和用户手工放行。

## 架构约束

- 保持模块化单体，工作流只承担粗粒度自动化，不参与逐 K 线计算或下单。
- 金融时间统一使用 UTC，金额、价格和数量使用 Decimal，禁止用 `float64` 表示账务值。
- 新接口放在 `/api/v1`，金融数值在 JSON 中使用字符串表示。
- 不为未进入当前里程碑的能力提前引入微服务、消息中间件或兼容层。
- 数据采集只抽取 Binance、OKX 真实共享的规范化类型和生命周期；工作流通过任务类型、参数 Schema 和资源 ID 调用领域能力；通知通过领域事件和统一消息调用渠道，不建设动态插件平台。
